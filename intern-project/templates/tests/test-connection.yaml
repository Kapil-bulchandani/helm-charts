apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "intern-project.fullname" . }}-test-connection"
  labels:
    {{- include "intern-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "intern-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
